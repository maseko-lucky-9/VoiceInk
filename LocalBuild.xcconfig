// LocalBuild.xcconfig
// Build configuration for local/unsigned builds (no Apple Developer certificate required).
// Used by `make local` — does NOT affect Debug or Release configurations.

// Signing identity is set on the `make local` command line (CODE_SIGN_IDENTITY),
// NOT here. The Makefile detects the stable "VoiceInk Local Signing" cert and uses
// it (so TCC grants persist across rebuilds), falling back to ad-hoc "-" if absent.
// Do NOT hardcode CODE_SIGN_IDENTITY here — an xcconfig value overrides the
// command-line override for code-signing settings and silently forces ad-hoc.
CODE_SIGN_STYLE = Manual
DEVELOPMENT_TEAM =
PROVISIONING_PROFILE_SPECIFIER =

// Entitlements are set via absolute path in the Makefile command line
// (relative paths break SPM package targets that resolve from their own source root)

// Add LOCAL_BUILD compilation flag so Swift code can use #if LOCAL_BUILD
SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) LOCAL_BUILD
